{
  "menu": {
    "home": "Home",
    "projects": "Projects",
    "blog": "Blog",
    "store": "Store",
    "contact": "Contact"
  },
  "a11y": {
    "skipToContent": "Skip to content"
  },
  "redirect": {
    "discord": "Redirecting you to our Discord server...",
    "instagram": "Redirecting you to our Instagram profile...",
    "twitch": "Redirecting you to our Twitch channel...",
    "kick": "Redirecting you to our Kick channel...",
    "youtube": "Redirecting you to our YouTube channel...",
    "twitter": "Redirecting you to our X profile...",
    "facebook": "Redirecting you to our Facebook page...",
    "tiktok": "Redirecting you to our TikTok profile..."
  },
  "meta": {
    "home": "PlayerBerry — a software studio designing and building end-to-end digital products across web, mobile and cloud.",
    "projects": "Selected production work touching real users: web, mobile, design and DevOps projects.",
    "blog": "Architecture decisions, design notes and lessons from the field — the PlayerBerry engineering journal.",
    "store": "The tools we use in our own projects: templates, component sets and starter kits.",
    "contact": "Get in touch with PlayerBerry to discuss your project, get a quote, or just say hi.",
    "about": "The PlayerBerry story: an independent software studio taking ideas to production with a small, senior team.",
    "legal": "Legal documents of the PlayerBerry website."
  },
  "hero": {
    "titleTop": "Not ordinary software —",
    "titleHighlight": "epic experiences.",
    "description": "We're a software studio building end-to-end digital products across web, mobile and cloud. We turn your idea into a product together, ship it, and keep growing it.",
    "ctaPrimary": "Explore Our Work",
    "ctaSecondary": "Get in Touch",
    "stats": {
      "years": { "value": "8+", "label": "Years of experience" },
      "projects": { "value": "40+", "label": "Projects shipped" },
      "clients": { "value": "25+", "label": "Happy clients" },
      "uptime": { "value": "99.9%", "label": "Uptime" }
    }
  },
  "home": {
    "services": {
      "eyebrow": "// what we do",
      "title": "End-to-end product development",
      "lead": "Design, engineering and infrastructure under one roof — you work with the same team from idea to launch.",
      "items": {
        "web": {
          "title": "Web Applications",
          "description": "Scalable, fast and accessible web applications built with Vue and TypeScript."
        },
        "mobile": {
          "title": "Mobile Applications",
          "description": "Truly native, App Store quality iOS experiences crafted with Swift and SwiftUI."
        },
        "design": {
          "title": "UI/UX Design",
          "description": "Design systems, prototypes and user-centered interfaces that set your product apart."
        },
        "devops": {
          "title": "Cloud & DevOps",
          "description": "Docker, CI/CD and cloud infrastructure to ship your product safely and keep it running."
        }
      }
    },
    "process": {
      "eyebrow": "// how we work",
      "title": "From idea to product in four steps",
      "lead": "The same discipline on every project: clear scope, fast feedback, measurable results.",
      "steps": {
        "discover": {
          "title": "Discovery",
          "description": "We clarify the problem, the audience and the success criteria together."
        },
        "design": {
          "title": "Design",
          "description": "Flows, prototypes and a design system — you see the product before code is written."
        },
        "build": {
          "title": "Development",
          "description": "Short sprints, continuous previews and testing; a working release every week."
        },
        "launch": {
          "title": "Launch & Support",
          "description": "We ship, monitor and grow. We leave when the product settles, not when the work stops."
        }
      }
    },
    "work": {
      "eyebrow": "// projects",
      "title": "Selected work",
      "lead": "A slice of production products across different industries.",
      "viewAll": "All projects"
    },
    "journal": {
      "eyebrow": "// blog",
      "title": "Latest from the blog",
      "lead": "We write down what we learn: architecture decisions, design notes, field experience.",
      "viewAll": "All posts"
    },
    "cta": {
      "title": "Have a project in mind?",
      "description": "Let's talk it through over a coffee — we'll shape the scope and roadmap together.",
      "button": "Get a Quote"
    }
  },
  "projects": {
    "eyebrow": "// portfolio",
    "title": "Projects",
    "lead": "Hand-picked work that is live in production, touching real users.",
    "categories": {
      "all": "All",
      "web": "Web",
      "mobile": "Mobile",
      "design": "Design",
      "devops": "DevOps"
    },
    "items": {
      "nova": {
        "title": "Nova Analytics",
        "description": "Real-time analytics dashboard for SaaS teams. Turns complex data into insight within seconds."
      },
      "pulse": {
        "title": "Pulse Fit",
        "description": "Workout tracking app with Apple Watch integration and full HealthKit synchronization."
      },
      "atlas": {
        "title": "Atlas Commerce",
        "description": "High-traffic e-commerce platform with Stripe payments and advanced catalog management."
      },
      "echo": {
        "title": "Echo Chat",
        "description": "Real-time messaging infrastructure with millisecond latency; WebSocket and Redis fan-out."
      },
      "forge": {
        "title": "Forge CI",
        "description": "Lightweight self-hosted CI/CD orchestrator with container-based isolated builds."
      },
      "orbit": {
        "title": "Orbit DS",
        "description": "Scalable, design-token driven design system for a multi-brand product family."
      }
    }
  },
  "blog": {
    "eyebrow": "// journal",
    "title": "Blog",
    "lead": "Architecture decisions, design notes and lessons from the field.",
    "readingTime": "{minutes} min read",
    "back": "All posts",
    "copy": "Copy",
    "copied": "Copied",
    "posts": {
      "swift-concurrency": {
        "title": "Swift Concurrency: Goodbye Callback Hell with async/await",
        "excerpt": "In an app we inherited for maintenance, we once counted six levels of nested closures. async/await reads the same flow like a straight story — here's how.",
        "body": [
          "A few years back we inherited an iOS app for maintenance and sat down to count: six levels of nested completion handlers in a single function. Nobody could tell which error belonged to which layer in the innermost closure; people were so afraid to touch it that someone had left a comment at the top saying 'do not enter'. Not a joke.",
          "## How the Ladder Gets Built",
          "The interesting part: whoever built that ladder wasn't a bad developer. In the old world, even the most innocent flow — fetch a user, then their avatar — inevitably ended up looking like this:",
          "```swift\nfunc loadProfile(id: String,\n                 completion: @escaping (Result<Profile, Error>) -> Void) {\n    fetchUser(id) { result in\n        switch result {\n        case .success(let user):\n            fetchAvatar(user.avatarURL) { avatarResult in\n                switch avatarResult {\n                case .success(let avatar):\n                    completion(.success(Profile(user: user, avatar: avatar)))\n                case .failure(let error):\n                    completion(.failure(error))\n                }\n            }\n        case .failure(let error):\n            completion(.failure(error))\n        }\n    }\n}\n```",
          "Twenty lines for two requests. The actual logic is three words — get user, get avatar, combine — yet it's nowhere to be seen. Every layer carries its own error handling, and if you forget to call `completion` on one branch, the app silently hangs. No compiler will ever tell you.",
          "## await: Wait, but Don't Block",
          "The same function with async/await:",
          "```swift\nfunc loadProfile(id: String) async throws -> Profile {\n    let user = try await fetchUser(id)\n    let avatar = try await fetchAvatar(user.avatarURL)\n    return Profile(user: user, avatar: avatar)\n}\n```",
          "Wherever you see `await`, the function pauses without blocking the thread; the system runs other work in the meantime. Error handling moved from closures to `throws`: no more unwrapping `Result` by hand, no more forgotten `completion`. If a branch is missing, you find out at compile time, not in production.",
          "## Don't Queue Up Independent Work",
          "In the code above the avatar depends on the user, so waiting in order is fair. But awaiting three independent requests one after another is plain waste. `async let` starts them all at once:",
          "```swift\nfunc loadDashboard(for id: String) async throws -> Dashboard {\n    async let profile = fetchProfile(id)\n    async let feed = fetchFeed(id)\n    async let badges = fetchBadges(id)\n\n    return try await Dashboard(profile: profile,\n                               feed: feed,\n                               badges: badges)\n}\n```",
          "Total time is now the slowest request, not the sum of all three. In that inherited project, this one change took the home screen from 1.8 seconds to 700 milliseconds. We wouldn't have believed it without measuring.",
          "## Shared State: Enter the Actor",
          "The part of concurrency that actually burns you is data races: two tasks writing to the same dictionary at the same time — sometimes a crash, sometimes silently corrupted data. Swift's answer is the `actor`: only one task can touch its state at a time, and the compiler guarantees it:",
          "```swift\nactor ImageCache {\n    private var store: [URL: Image] = [:]\n\n    func image(for url: URL) -> Image? {\n        store[url]\n    }\n\n    func insert(_ image: Image, for url: URL) {\n        store[url] = image\n    }\n}\n\n// Access always goes through await:\nlet cache = ImageCache()\nawait cache.insert(avatar, for: url)\nlet hit = await cache.image(for: url)\n```",
          "We used to build this with locks on a `DispatchQueue` and hope everyone used them correctly. Hope is not a concurrency strategy.",
          "> async/await doesn't make code shorter; it makes it readable. The win isn't in the line count — it's on the face of whoever opens that file six months from now.",
          "That 'do not enter' function is thirty lines today, and every new hire understands it on first read. We haven't met anyone who misses the callback ladder."
        ]
      },
      "modern-cpp-bellek": {
        "title": "Modern C++: Stop Managing Memory by Hand",
        "excerpt": "Hunting a leak through valgrind output at 2 a.m. is certainly an experience; we don't recommend it. RAII and smart pointers retire most of those hunts for good.",
        "body": [
          "If you've ever hunted a memory leak through valgrind output at two in the morning on a game server, you already know what this post is about. The culprit was almost always the same: a `new` somewhere, and one of the return paths skipping the `delete`. C++'s raw power over memory collects its toll exactly like this.",
          "## How a Leak Is Born",
          "Look at this function — it could be from a textbook:",
          "```cpp\nvoid process() {\n    Widget* w = new Widget();\n\n    if (!w->init()) {\n        return;            // leak: delete is never reached\n    }\n\n    w->run();\n    delete w;\n}\n```",
          "If `init()` fails, `delete` never runs and the `Widget` is orphaned in memory. If `run()` throws, same story. The problem isn't carelessness — it's entrusting 'clean up on every exit path' to a human. Humans forget. The bigger the codebase, the more they forget.",
          "## RAII: Let the Object Do the Cleanup",
          "C++'s answer has a bureaucratic-sounding name — Resource Acquisition Is Initialization — but the idea is one sentence: tie the resource to an object's lifetime. When the object leaves scope, its destructor runs and releases the resource. How you leave doesn't matter — normal flow, early `return`, an exception... the destructor always runs:",
          "```cpp\nclass FileHandle {\npublic:\n    explicit FileHandle(const char* path)\n        : file_(std::fopen(path, \"r\")) {}\n\n    ~FileHandle() {\n        if (file_) std::fclose(file_);\n    }\n\n    // Copying is disabled so two objects never close the same file.\n    FileHandle(const FileHandle&) = delete;\n    FileHandle& operator=(const FileHandle&) = delete;\n\n    std::FILE* get() const { return file_; }\n\nprivate:\n    std::FILE* file_;\n};\n```",
          "There's no 'remember to close the file' task anymore; the moment a `FileHandle` leaves scope, the file closes. `std::lock_guard`, `std::vector`, `std::string` — the standard library is built on this same pattern. You've probably been using RAII for years without naming it.",
          "## Smart Pointers: Life Without new/delete",
          "The same pattern comes prepackaged for pointers: `unique_ptr` for single ownership, `shared_ptr` for shared ownership.",
          "```cpp\n#include <memory>\n\nauto w = std::make_unique<Widget>();\nif (!w->init()) {\n    return;               // no leak; w cleans up after itself\n}\nw->run();\n\n// Shared ownership: the object is freed when the last owner goes.\nauto config = std::make_shared<Config>();\nuseConfig(config);\n```",
          "The entire bug class from the original `process()` — the forgotten `delete`, the double `delete`, the leak on the exception path — disappeared with a one-line change. The performance cost? Zero for `unique_ptr`; the compiled code is identical to a raw pointer.",
          "One honest warning: `shared_ptr` is not a cure-all. Its reference count is atomic, so it isn't free, and cyclic references — A holds B, B holds A — never let the count reach zero. Make `unique_ptr` your default; reach for `shared_ptr` only when ownership genuinely is shared, and use `weak_ptr` on the edge that risks a cycle.",
          "> If you see naked new and delete in modern C++ code, either something very special is going on or a bug is waiting. Experience says: usually the latter.",
          "Back to that 2 a.m. valgrind session: we fixed that leak by hand, because the codebase wasn't ready for smart pointers. The next month we made the rule — new code doesn't write naked `new`. We haven't been on a leak hunt since."
        ]
      },
      "c-isaretciler": {
        "title": "Pointers in C: Feared, but Misunderstood",
        "excerpt": "A pointer boils down to a single idea: store the address, not the value. Everything else — arrays, pass-by-reference, const — follows from it.",
        "body": [
          "Everyone learning C hits the wall in the same week: pointer week. Asterisks, ampersands, cryptic compiler warnings... Yet behind the curtain there's a single idea: store not a variable's value, but where that value lives in memory. Everything else is a consequence.",
          "## & Gives the Address, * Follows It",
          "```c\nint x = 42;\nint *p = &x;          // p holds x's address\n\nprintf(\"%d\\n\", *p);   // 42 — the value p points at\n*p = 7;               // x is now 7; p points at it\n```",
          "`&x` answers 'where does x live'; `*p` answers 'what's at the place p points to'. They're inverses. When you write `*p = 7`, you're changing `x` directly, because both look at the same address.",
          "The first real payoff of this idea is functions that can modify outside variables. Every argument in C is passed by copy — but pass an address and the function reaches the real data:",
          "```c\nvoid swap(int *a, int *b) {\n    int tmp = *a;\n    *a = *b;\n    *b = tmp;\n}\n\nint i = 1, j = 2;\nswap(&i, &j);         // i == 2, j == 1\n```",
          "If you've ever wondered why `scanf` needs `&num`: that's the answer. You hand it the address so it can write the value it reads into your variable.",
          "## Arrays: Closer Kin Than They Look",
          "The fact that surprises C beginners the most: an array name, in most contexts, turns into the address of its first element. This is called 'decay':",
          "```c\nint nums[3] = {10, 20, 30};\nint *p = nums;              // same as &nums[0]\n\nprintf(\"%d\\n\", *(p + 2));   // 30\nprintf(\"%d\\n\", p[2]);       // 30 — exactly the same thing\n```",
          "`p[2]` and `*(p + 2)` are two spellings of the same expression; the brackets are just shorthand. The subtlety people miss: `p + 2` advances two `int`s, not two bytes. Pointer arithmetic accounts for the element size on your behalf.",
          "## const: A Promise to the Compiler",
          "Every function that takes a pointer also receives the power to modify the data. `const` is how you hand that power back:",
          "```c\nvoid printName(const char *name) {\n    // *name = 'X';  -> compile error\n    printf(\"Hello, %s\\n\", name);\n}\n```",
          "One tiny keyword doing two jobs: it guarantees the caller you won't touch their data, and it gives you a compile error if you try by accident. Free insurance — be generous with it in your signatures.",
          "The bad reputation of pointers comes, I think, from bad teaching. Diving into syntax before the address concept settles — without drawing boxes and arrows — turns the symbols into meaningless magic. Once the address idea clicks, though, arrays, strings, dynamic memory and linked lists all stack neatly on the same foundation. Nothing to fear; this is the heart of C."
        ]
      },
      "llm-urun-entegrasyonu": {
        "title": "Adding an LLM to Your Product: the Gap Between Demo and Production",
        "excerpt": "Friday's demo got applause; on Monday the first real user cornered the system in three minutes. Four disciplines that close that gap.",
        "body": [
          "We showed the demo on Friday afternoon and the room applauded. On Monday morning the first real user uploaded a screenshot instead of an invoice, refreshed the page mid-response, and surfaced four distinct failures we'd never seen in the demo — in three minutes. Productizing an LLM is, in short, the distance between those two days.",
          "## Same Question, Two Different Answers",
          "The software we're used to produces the same output for the same input. A language model doesn't — and instead of fighting that, you build the architecture around it. Rule one: never trust the model's output raw. If you expect structured data, validate against a schema and retry on mismatch:",
          "```ts\nconst Invoice = z.object({\n  vendor: z.string(),\n  total: z.number().positive(),\n  currency: z.enum([\"TRY\", \"USD\", \"EUR\"]),\n});\n\nasync function extractInvoice(text: string) {\n  for (let attempt = 1; attempt <= 3; attempt++) {\n    const raw = await model.complete(prompt(text));\n    const parsed = Invoice.safeParse(tryJson(raw));\n    if (parsed.success) return parsed.data;\n  }\n  throw new ExtractionFailed();\n}\n```",
          "When three attempts still don't fit the schema, the right move is an honest error to the user — not silently accepting bad data. A system that can say 'I'm not sure' earns far more trust than one that speaks confidently without being sure.",
          "## Context Beats the Model",
          "The sentence we hear most on projects: 'Would a stronger model fix this?' Usually no — because what determines answer quality is mostly what you show the model. In a RAG setup the real engineering lives in the retrieval layer: a system that can't find the right document will fabricate, whatever the model. It has no other material to work with.",
          "Stuffing the context window isn't the fix either. Ten irrelevant documents distract the model and inflate the bill. In our measurements, two relevant paragraphs consistently beat twenty added 'just in case'. Less, but on target, wins.",
          "## Don't Change Prompts Blind",
          "You changed three words in the prompt; is the system better now? Without measurement, the honest answer is 'no idea'. For every critical flow we keep an evaluation set compiled from real user questions — inputs and expected behaviors, an ordinary file in version control:",
          "```json\n{\n  \"input\": \"Who approved last month's AWS invoice?\",\n  \"expect\": {\n    \"must_cite_source\": true,\n    \"must_not_contain\": [\"I guess\", \"probably\"],\n    \"tool_called\": \"search_invoices\"\n  }\n}\n```",
          "Whenever the prompt or the model changes, the set reruns; if the score drops, the change doesn't reach the main branch. This is the regression test of the LLM world. It took half a day to set up; in return, deployments stopped being gambles.",
          "## The Token Bill and the Three-Second Void",
          "Nobody looks at cost in a demo; in production every request is real money. The most effective remedy is routing: send classification and short summaries to a small model, and save the big one for genuinely hard questions. That single decision cut our bill by more than half. On the latency side, streaming is non-negotiable: if the first word lands in 300 milliseconds, users don't mind a three-second total. Three seconds staring at a blank screen, though, is an eternity.",
          "> A demo is a showcase of the happy path; a product is the sum of the unhappy ones. With LLMs, that gap is wider than ever.",
          "The chasm can be crossed. Schema validation, targeted context, an evaluation set and cost discipline — together they turn a mesmerizing demo into a dependable product. In order, and without rushing."
        ]
      },
      "core-web-vitals": {
        "title": "Core Web Vitals: How Users Actually Feel Speed",
        "excerpt": "Lighthouse said 90; users said the site was slow. Both were right. What the three metrics mean in the field, and how they actually got fixed.",
        "body": [
          "An e-commerce client came to us with a 'the site is slow' complaint. We opened Lighthouse: above 90, all green. Then we looked at real user data and the picture flipped — on mid-range Android phones, LCP was over four seconds. Both were true: the lab was fast, the field was slow. That's exactly where Core Web Vitals earn their keep: they measure what users actually feel.",
          "## LCP: When Did the Page 'Arrive'",
          "Largest Contentful Paint marks the moment the biggest piece of content — usually the hero image or the headline — shows up. In that project the culprit was a late-discovered hero image: the browser downloaded the CSS first and only then learned the image existed. The cure is telling it up front:",
          "```html\n<link\n  rel=\"preload\"\n  as=\"image\"\n  href=\"/hero.avif\"\n  imagesrcset=\"/hero-800.avif 800w, /hero-1600.avif 1600w\"\n/>\n```",
          "Then there's the webfont classic: sites that hide text until the font arrives show users a long blank stare. `font-display: swap` renders with a system font first and switches when the brand font lands:",
          "```css\n@font-face {\n  font-family: \"Space Grotesk\";\n  src: url(\"/fonts/space-grotesk.woff2\") format(\"woff2\");\n  font-display: swap;\n}\n```",
          "Those two changes plus converting images to AVIF took that site's LCP from 4.1 seconds to 1.9. Nothing exotic — just making the browser's job easier.",
          "## INP: I Clicked, Why Is Nothing Happening",
          "Interaction to Next Paint measures the time from an interaction to the first visible response. The chief enemy is JavaScript that locks the main thread for hundreds of milliseconds: while the browser is busy with one long task, clicks queue up and the UI feels frozen. The cure is splitting the work:",
          "```js\nasync function processRows(rows) {\n  for (const [i, row] of rows.entries()) {\n    handle(row);\n    if (i % 100 === 0) {\n      // Let the browser breathe every 100 rows:\n      await new Promise((r) => setTimeout(r, 0));\n    }\n  }\n}\n```",
          "Crude but effective: the long loop now yields the main thread periodically, and pending clicks get processed. There are nicer tools — `scheduler.yield()`, Web Workers — but the principle never changes: the main thread belongs to the user; you're a guest there.",
          "## CLS: I Was About to Tap It, and It Moved",
          "Cumulative Layout Shift measures how much content jumps around while loading. Everyone has lived it: the paragraph you're reading dives down because a banner popped in above. The cause is almost always the same — images and embeds with no declared size. In modern CSS the fix is two lines:",
          "```css\nimg {\n  aspect-ratio: 16 / 9; /* space is reserved before the image loads */\n  width: 100%;\n  height: auto;\n}\n```",
          "The moment the browser knows the aspect ratio, it reserves the spot up front; nothing jumps. Same principle for dynamic banners and ads: reserve the area first, drop the content into it.",
          "> Lab tests measure your computer; RUM measures your user's phone. When deciding, look at the second one.",
          "The lesson that project left us with: performance isn't a one-off spring cleaning, it's managed like a budget. Every new feature spends from it. Keep measuring and the debt stays visible before it piles up; stop, and it comes back with interest."
        ]
      },
      "typescript-tip-guvenligi": {
        "title": "Making Invalid States Impossible with TypeScript",
        "excerpt": "If refactoring scares you, the problem may not be your courage — it may be your types. On using the type system as a design tool, not a labeling machine.",
        "body": [
          "If the room goes quiet when someone says 'let's rename this field', the problem isn't courage — it's the types. In a well-modeled codebase, refactoring is fearless: you make the change, everything broken lights up red, you clear the list, done. Teams that use TypeScript as 'JavaScript with labels' never get to feel that ease.",
          "## The Boolean Swamp",
          "The classic example, because it's still everywhere: three separate fields — `isLoading`, `isError`, `data`. Three fields give you eight combinations; three of them are meaningful. A state that's both loading and failed doesn't logically exist — but it exists in the type system, and one day it shows up in production. Model the state as a discriminated union and the absurd combinations drop out of the language:",
          "```ts\ntype State =\n  | { status: \"loading\" }\n  | { status: \"error\"; error: Error }\n  | { status: \"success\"; data: User };\n```",
          "`data` is now reachable only on the `success` branch. Code that tries to read it while loading doesn't even compile; that whole bug class dies in the editor, long before production.",
          "The lesser-known bonus of this pattern: combined with `switch`, the compiler catches missing branches too:",
          "```ts\nfunction render(state: State) {\n  switch (state.status) {\n    case \"loading\":\n      return spinner();\n    case \"error\":\n      return alert(state.error);\n    case \"success\":\n      return table(state.data);\n    default: {\n      const missing: never = state; // breaks here if a new state is added\n      return missing;\n    }\n  }\n}\n```",
          "Six months later, when someone adds `{ status: \"empty\" }` to `State`, this `switch` fails to compile and every place that needs updating reports itself. The compiler writes your to-do list.",
          "## When Everything Is a string",
          "User id: `string`. Email: `string`. Order number: `string`... When they're all the same type, nothing stops you from writing `sendEmail(user.id)`. Branded types turn that mix-up into a compile error:",
          "```ts\ntype UserId = string & { readonly __brand: \"UserId\" };\ntype Email = string & { readonly __brand: \"Email\" };\n\nfunction sendEmail(to: Email) {\n  /* ... */\n}\n\nsendEmail(user.id); // compile error — an id is not an Email\n```",
          "At runtime these are ordinary strings; the cost is zero. But for values where a mix-up is expensive — money, dates, units of measure — this zero-cost fence prevents real accidents. The Mars Climate Orbiter was lost to a metric-imperial mix-up; a well-shaped type system would have caught that before launch.",
          "## Validate at the Boundary, Trust Inside",
          "Types only hold inside your own code. Calling `as User` on JSON from an API is not a fact — it's a wish. Do runtime validation at the boundary; inside, trust your types with a clear conscience:",
          "```ts\nconst User = z.object({\n  id: z.string(),\n  name: z.string(),\n  age: z.number().int().min(0),\n});\n\nconst user = User.parse(await res.json()); // blows up here if it doesn't fit\n```",
          "Now the failure happens at the door where the data enters, with a readable message — not three layers deep as 'undefined is not a function'.",
          "> `as` is an expression of hope; `parse` is proof. Demand proof at the boundary.",
          "Once you start treating types as a design tool, something interesting happens: you see the gaps in your design while modeling, before writing any code. The compiler makes you answer 'what happens to this field in that state?'. A type definition that feels bad to write is very often the first symptom of a bad design decision."
        ]
      },
      "erisilebilirlik-varsayilan": {
        "title": "Accessibility Is Not a Layer You Add Later",
        "excerpt": "Put the mouse in a drawer and try navigating your site with Tab alone. That five-minute test says more about most interfaces than a month of analytics.",
        "body": [
          "During a user test, we watched a participant navigate our site with a screen reader. Ten minutes surfaced five problems we hadn't noticed in months. The most painful one: what we thought was an 'Add to cart' button was, to him, just 'something clickable' — a nameless box that never said what it did. That was the day we understood accessibility isn't a compliance item; it's product quality, plain and simple.",
          "## The Five-Minute Test: Drop the Mouse",
          "The fastest read on your site's state: put the mouse away and navigate with Tab, Enter and the arrow keys only. Can you see where focus is? Can you reach every button? Can you close that menu with Escape? An interface that fails this test is broken not just for disabled users but for everyone who prefers working from the keyboard.",
          "The mistake this test catches most often is well known: building buttons out of `div`s.",
          "```html\n<!-- Looks like a button, does none of the job: -->\n<div class=\"btn\" onclick=\"save()\">Save</div>\n\n<!-- Keyboard, focus, Enter, screen reader — all free: -->\n<button type=\"button\" onclick=\"save()\">Save</button>\n```",
          "The first one works with a mouse and stops there: it can't take focus, doesn't hear Enter, never tells a screen reader it's a button. You could patch all of that back with `tabindex`, `role` and `keydown` — but why? The right HTML element does with zero lines what a hundred lines of ARIA patchwork can't.",
          "## Structure Is Told, Not Shown",
          "A screen reader user doesn't scan the page visually; they listen, jumping between headings and landmarks. That's why `<nav>`, `<main>`, `<header>` and a sane heading hierarchy aren't a 'clean code' aesthetic — they're an actual navigation instrument. Alt text is part of the same job, and a craft of its own: 'chart' carries no information; 'line chart showing revenue up 20 percent in Q4' does. The rule is simple: don't describe the image, convey what it says.",
          "## Color Can't Speak Alone",
          "If you marked invalid form fields with red alone, then for a color-blind user you marked nothing — and that's one in twelve men, not a rounding error. Color always needs a second channel: an icon, a label, a pattern. Contrast is equally non-negotiable; that elegant light-gray text disappears on a phone screen at noon.",
          "Motion is less talked about, but for some users it's not comfort — it's health: large parallax and sweeping transitions can trigger genuine physical nausea in people with vestibular disorders. Respecting the user who asked their system to reduce motion is one media query:",
          "```css\n@media (prefers-reduced-motion: reduce) {\n  *,\n  *::before,\n  *::after {\n    animation-duration: 0.01ms !important;\n    transition-duration: 0.01ms !important;\n  }\n}\n```",
          "> Captions were invented for deaf users; today we all watch videos with them on the subway. What's designed for the constraint gets clearer for everyone.",
          "Since that user test, the team rule is: every new component gets keyboard-tested before merging, every image arrives with its alt text, every color is chosen with its contrast. On paper it looks like extra work; in practice, next to the cost of fixing it later, doing it right up front is nearly free."
        ]
      },
      "vue-3-5-performans": {
        "title": "Vue 3.5 Reactivity: Practical Performance Notes",
        "excerpt": "A dashboard that froze while typing over a 5,000-row table became smooth with three changes. No magic — just knowing how reactivity works.",
        "body": [
          "A few months ago we worked on a classic complaint in an analytics dashboard: the UI froze while a 5,000-row table updated. Moving to Vue 3.5 helped by itself — the new reactivity internals cut memory use substantially — but the real difference came from three deliberate changes. All three hide in the answer to the same question: how does reactivity actually work?",
          "## Don't Deep-Track Everything",
          "By default Vue makes every level of an object reactive. Attaching a tracker to each of 5,000 chart points just burns memory — you never mutate those points individually anyway. If you replace the data wholesale, tracking the reference alone is enough:",
          "```ts\nimport { shallowRef } from \"vue\";\n\nconst points = shallowRef<Point[]>([]);\n\n// Replace wholesale when new data arrives — one trigger:\nasync function refresh() {\n  points.value = await fetchPoints();\n}\n```",
          "The rule in practice: if you reassign rather than mutate, use `shallowRef`. In our dashboard this was the single biggest win; memory visibly dropped and the stutter on large updates disappeared.",
          "## Fat computed, Lazy Chain",
          "The second culprit was one giant `computed` doing everything: filter, sort, group, sum — all in one. Any dependency twitching reran the whole chain. We split it:",
          "```ts\n// Before: everything in one computed, everything reruns on any change.\n// After: each link runs only when its own input changes.\nconst filtered = computed(() => rows.value.filter(matches(query.value)));\nconst sorted = computed(() => sortBy(filtered.value, sortKey.value));\nconst total = computed(() => sum(sorted.value, \"amount\"));\n```",
          "When the sort key changes, filtering no longer repeats; only `sorted` and downstream recompute. Small computeds are both faster and more readable — nobody could tell what was going on inside the giant one anyway.",
          "## v-memo: Powerful, but Sharp",
          "The final touch: `v-memo` on the table rows. A row skips re-rendering as long as its dependencies haven't changed:",
          "```html\n<tr\n  v-for=\"row in rows\"\n  :key=\"row.id\"\n  v-memo=\"[row.id === selectedId, row.updatedAt]\"\n>\n  ...\n</tr>\n```",
          "When the selection changes, only two of the 5,000 rows — the previously and newly selected — re-render. But let's be honest: `v-memo` is a sharp knife. Get the dependency list wrong and you've told Vue 'never update this row'; the UI goes silently stale. We use it only on a measured bottleneck — never 'just in case'.",
          "> Measure first, then optimize — but once you've measured, don't hesitate either. Optimization by guesswork creates more problems than it solves.",
          "The outcome: first paint halved, the typing freezes gone, and the code more readable than before. That's the satisfying part of performance work — once the diagnosis is right, the fix is usually a few lines."
        ]
      },
      "tasarim-sistemi-kurmak": {
        "title": "Building a Design System from Scratch: Tokens to Components",
        "excerpt": "Our first system started with a color palette and collapsed into 14 button variants within three months. What keeps the second one standing isn't colors — it's a three-layer token architecture and one hard rule.",
        "body": [
          "We built our first design system by picking a color palette. Six weeks later came the first 'special case': a campaign page wanted the button 'just a bit darker'. Then another. Three months in, the system had 14 button variants and nobody knew which one to use when. On the second attempt we started not with color, but with decisions.",
          "## Decisions, Not Values",
          "What a design system actually standardizes isn't colors — it's decisions: how many variants does a button need? When does a card get a shadow? When is the error color used — and more importantly, when is it not? A palette picked before answering those questions is a pretty color collection, nothing more. Values are the output of decisions, not the input.",
          "## Three Layers: Raw, Semantic, Component",
          "Our token architecture has three layers, and components never look at raw values — only at semantic roles:",
          "```less\n// 1) Raw values — no meaning, just constants:\n@berry-500: #ff3d77;\n@ink-900: #0a0a10;\n\n// 2) Semantic roles — 'what is this value for':\n@color-danger: @berry-500;\n@surface-base: @ink-900;\n\n// 3) Component tokens — one specific use:\n@button-danger-bg: @color-danger;\n```",
          "At first glance it looks like a needless indirection — until the first theming request arrives. Dark theme, branded white theme, high contrast: all of them come out of changing only the second layer, without touching a line of component code. The moment you write `#ff3d77` inside a component, that flexibility is gone; the value now lives there and won't travel.",
          "## Resist Variant Inflation",
          "On the component side the principle is the same: a `Button` offers 'primary, secondary, danger' — not 'blue button, red button'. The designer says 'this action is destructive', the developer picks the `danger` variant; what color that is stays the system's internal business, not the page's.",
          "Keeping the variant count low is a conscious fight. Every new variant is another surface to test, document and keep consistent. Those 14 buttons in our first system taught us the lesson: everything added 'just in case' becomes the fastest-rotting corner of the system.",
          "## The One Hard Rule",
          "The rule that has kept the system alive for two years is one sentence: no value that isn't in the design enters the code, no component that isn't in the code enters the design. Both sides feed from the same token source. The rule gets annoying at times — the 'it's a two-minute job, I'll just hardcode it' temptation never dies — but those two-minute jobs are exactly what kills systems. Special-case CSS is the first sign of rot; let one in and more will follow.",
          "> A design system isn't a project — it's an agreement. Projects end; an agreement lives as long as both sides honor it.",
          "Our second system has been growing for six months without a single line of special-case CSS. Opening a new page no longer means writing CSS; it means arranging existing components. And everything we learned from the wreck of the first system sits under this one — which might be the real first step of building a design system: crashing one."
        ]
      },
      "swiftui-ile-native": {
        "title": "Interfaces That Feel Native with SwiftUI",
        "excerpt": "The honest answer to 'couldn't we just use Flutter?' — you could, but that feel won't be there. The three details behind the native feel, and how we handle them in SwiftUI.",
        "body": [
          "Nearly every project kickoff includes the same question: 'Couldn't we go cross-platform?' The honest answer: you could — but that feel won't be there. An app feeling native comes down to three details: the curve of its transition animations, the timing of its haptic feedback, and its scroll physics. Users can't name them, but they sense the difference instantly. SwiftUI's edge is that it takes all three straight from the platform; it doesn't imitate them, it uses them.",
          "## Don't Reinvent the Spring",
          "iOS's spring animations are the product of a decade-plus of tuning. An animation with the right duration but the wrong curve leaves users with a 'something's off' feeling they can't explain. With the real thing available, there's no reason to draw curves by hand:",
          "```swift\nwithAnimation(.spring(response: 0.35, dampingFraction: 0.8)) {\n    isExpanded.toggle()\n}\n```",
          "Two parameters, and the animation speaks the platform's language. Haptic feedback lives in the same cheap-win category: a tiny, well-timed tap — `sensoryFeedback(.success, trigger:)` — gives an action the feeling that it 'really happened'. These aren't decoration; they're the building blocks of user trust.",
          "## Keep Views Dumb",
          "The one principle we hold onto tightly in architecture: views are dumb. They know no business logic; they take data and draw. State management lives in the container layer, navigation in its own:",
          "```swift\nstruct AvatarView: View {\n    let user: User\n\n    var body: some View {\n        HStack(spacing: 12) {\n            Circle()\n                .fill(.pink.gradient)\n                .frame(width: 44, height: 44)\n            Text(user.name)\n                .font(.headline)\n        }\n    }\n}\n```",
          "The everyday payoff of this split shows up in previews: a dumb view can be previewed on its own, without booting the app. We sit with the designer looking at the same screen and settle the 'should this gap be 4 or 8' debate on the code, instantly. Previews became our living design document; the 'is it actually the same?' gap between Figma and code closed.",
          "## Knowing Where the Border Is",
          "SwiftUI has matured, but it's not the weapon for every battle. Text editors that demand millimetric control, camera interfaces, very custom scroll behaviors — there we still drop down to UIKit and bridge with `UIViewRepresentable`. There's no defeat in that; the craft is knowing where to draw the line. A dogmatic 'everything must be SwiftUI' stance is the most reliable way to turn a simple task into an epic.",
          "> The question is no longer 'can SwiftUI do it?' — it's 'where is it done cleanest?'",
          "Where things stand today: the native feel comes nearly free, and you spend your effort on your own product decisions instead. A one-person team that respects the details can ship something App Store quality in weeks. 'Wouldn't Flutter have worked?' It would have. But the difference is felt in those first three seconds users can't put into words."
        ]
      },
      "monorepo-mimarisi": {
        "title": "Monorepo Architecture: One Repo, Many Products",
        "excerpt": "A field got renamed in the API; web updated, mobile was forgotten, and the bug blew up in production three days later. That week we decided to fold three repos into one.",
        "body": [
          "The event that made the decision for us: a field in the API got renamed. The web team updated the same day; the counterpart in the mobile repo slipped through. The bug surfaced three days later, in production, as 'my profile page is empty' messages landing in customer support. Three repos, three versions, pull requests waiting on each other... That week we decided to move everything that shares a contract into a single repo.",
          "## The Comfort of Atomic Change",
          "The real promise of a monorepo is one word: atomicity. Rename a field in the API schema and the web and mobile code consuming it update in the same pull request. Whatever breaks is right there in the diff, in front of your eyes. In separate repos the same job becomes a version-bump dance: publish the package, bump it in three repos, keep a table of which version is compatible with which. In one repo, the main branch is consistent with itself at every moment — for us, that was the sentence that ended the architecture debate.",
          "## The Setup: Less Than You'd Think",
          "Our recipe is pnpm workspaces + Turborepo; the core is two small files:",
          "```yaml\n# pnpm-workspace.yaml\npackages:\n  - \"apps/*\" # web, mobile, api\n  - \"packages/*\" # shared: ui, config, api-client\n```",
          "```json\n{\n  \"tasks\": {\n    \"build\": {\n      \"dependsOn\": [\"^build\"],\n      \"outputs\": [\"dist/**\"]\n    },\n    \"test\": { \"dependsOn\": [\"build\"] }\n  }\n}\n```",
          "`^build` means: before building a package, build what it depends on. Turborepo works out the task graph and serves everything unchanged from cache. As the repo grows this cache becomes gold — the repo tripled in size and our CI is still faster than on day one.",
          "## The Real Issue: Layer Discipline",
          "The bad news: tools won't save you. Where monorepos collapse isn't technical, it's disciplinary — boundaries blur, everyone reaches into every package, and six months later you're holding a dependency tangle the size of a repo. Our rule is one-directional: apps may depend on packages; packages never on apps. And we entrust the rule to lint, not to people:",
          "```json\n{\n  \"rules\": {\n    \"import/no-restricted-paths\": [\"error\", {\n      \"zones\": [\n        { \"target\": \"./packages\", \"from\": \"./apps\" }\n      ]\n    }]\n  }\n}\n```",
          "Once the ban is automated, the debate ends too: a violation is a red CI mark, not a difference of opinion in code review. People spend their energy on the work, not on remembering rules.",
          "> As the repo grows, add automation, not rules. A rule enforced by tools is permanent; a rule people remember lasts until that person goes on holiday.",
          "One honest footnote: a monorepo isn't free. CI takes real effort to set up, git history gets crowded, and misconfigured, everyone ends up waiting on everyone. For products that don't share a contract and live separate lives, we still recommend separate repos. But ever since that three-day production bug, we can't accept code that speaks the same contract living in different houses."
        ]
      }
    }
  },
  "store": {
    "eyebrow": "// digital products",
    "title": "Store",
    "lead": "We package the tools we use in our own projects: templates, component sets and starter kits.",
    "buy": "Buy Now",
    "soon": "Coming soon",
    "badges": {
      "popular": "Popular",
      "new": "New"
    },
    "items": {
      "berry-ui": {
        "category": "Design System",
        "title": "Berry UI Kit",
        "description": "Dark-themed Less design system built on UIkit; 40+ ready-made blocks and a token architecture."
      },
      "vue-launch": {
        "category": "Template",
        "title": "Vue Launch",
        "description": "Vue 3 + TypeScript + Vite starter template; ships with i18n, router and a test setup."
      },
      "swift-components": {
        "category": "iOS",
        "title": "Swift Components",
        "description": "SwiftUI component pack: charts, cards and animated controls. Fully previewable."
      },
      "icon-forge": {
        "category": "Icon Set",
        "title": "Icon Forge",
        "description": "400+ pixel-perfect icons in SVG and font formats, in two weights."
      },
      "api-boiler": {
        "category": "Backend",
        "title": "API Boiler",
        "description": "Fastify + PostgreSQL API skeleton; authentication, schema validation and OpenAPI included."
      },
      "berry-blocks": {
        "category": "Template",
        "title": "Berry Blocks",
        "description": "25 ready-made landing blocks for marketing sites; copy, adapt, publish."
      }
    }
  },
  "contact": {
    "eyebrow": "// contact",
    "title": "Say hello",
    "lead": "Write to us to discuss your project, get a quote, or just to say hi.",
    "note": "We usually reply within 24 hours."
  },
  "about": {
    "eyebrow": "// about us",
    "title": "Small team, ambitious work",
    "lead": "PlayerBerry is an independent software studio that takes ideas to production. No agency crowd — you talk to the people doing the work.",
    "story": [
      "We started in 2018 with a single question: can good design, solid engineering and honest communication live in one team? Since then we've taken dozens of products from idea to launch across web, mobile and cloud.",
      "Instead of scaling into a big agency, we chose to stay small and senior. On every project you talk directly to the person building it — no layers in between. Decisions are fast, and feedback turns into code the same day.",
      "We leave when the product settles, not when the work stops. For us success isn't delivered code; it's a product that serves its users, stays easy to maintain and keeps growing over time."
    ],
    "values": {
      "craft": {
        "title": "Craft",
        "description": "Every detail counts. From pixel alignment to database indexes, we sweat the small stuff."
      },
      "clarity": {
        "title": "Clarity",
        "description": "Scope, timeline and cost are clear from day one. No surprise invoices, no hidden assumptions."
      },
      "partnership": {
        "title": "Partnership",
        "description": "We work as part of your team, not as a vendor. Your goal is our goal."
      },
      "longevity": {
        "title": "Longevity",
        "description": "We write code that's fast today and easy to maintain tomorrow — so the product lasts for years."
      }
    },
    "cta": {
      "title": "Let's work together",
      "description": "Tell us your idea and we'll shape the scope and roadmap with you.",
      "button": "Get in Touch"
    }
  },
  "notFound": {
    "title": "Page not found",
    "description": "The page you are looking for may have moved or never existed.",
    "button": "Back to home"
  },
  "footer": {
    "tagline": "A software studio building end-to-end digital products across web, mobile and cloud.",
    "nav": "Site",
    "rss": "RSS Feed",
    "legal": "Legal",
    "follow": "Follow us",
    "about": "About",
    "privacy": "Privacy Policy",
    "cookies": "Cookies Policy",
    "terms": "Terms of Service",
    "contact": "Contact",
    "randomized": {
      "part0": "No ",
      "part1": " was harmed in the making of this site."
    },
    "rights": "All rights reserved"
  }
}
